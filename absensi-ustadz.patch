diff --git a/app/institusi/[id]/layout.tsx b/app/institusi/[id]/layout.tsx
index 0046de4..71a0fb4 100644
--- a/app/institusi/[id]/layout.tsx
+++ b/app/institusi/[id]/layout.tsx
@@ -104,11 +104,17 @@ export default async function InstitusiLayout({
           <NavItem href={`/institusi/${institusi.id}/kategori`}>
             {isAdmin ? 'Kategori' : 'Kategori saya'}
           </NavItem>
-          {isAdmin && (
-            <NavItem href={`/institusi/${institusi.id}/laporan`}>
-              Laporan
-            </NavItem>
-          )}
+          {/*
+            Laporan berisi rekap kehadiran santri. Halaman /laporan sudah
+            meng-scope datanya sendiri: ustadz hanya melihat santri ampuannya
+            dan kategori yang ia ajar. Jadi tidak perlu digate isAdmin di sini.
+          */}
+          <NavItem href={`/institusi/${institusi.id}/laporan`}>
+            {isAdmin ? 'Laporan' : 'Laporan & absensi'}
+          </NavItem>
+          <NavItem href={`/institusi/${institusi.id}/target`}>
+            {isAdmin ? 'Target ustadz' : 'Target saya'}
+          </NavItem>
           {isPondok && (
             <NavItem href={`/institusi/${institusi.id}/recap`}>Recap poin</NavItem>
           )}
diff --git a/app/institusi/[id]/santri/[santriId]/santri-detail-client.tsx b/app/institusi/[id]/santri/[santriId]/santri-detail-client.tsx
index a3735ea..8b8775e 100644
--- a/app/institusi/[id]/santri/[santriId]/santri-detail-client.tsx
+++ b/app/institusi/[id]/santri/[santriId]/santri-detail-client.tsx
@@ -633,33 +633,39 @@ function ProgresForm({
         </div>
       )}
 
-      {progresType === 'kitab' && (
-        <>
-          <div>
-            <label className="block text-xs font-medium text-ink-700 mb-2">
-              Absen
+      {/*
+        Kehadiran WAJIB tampil di semua kategori, bukan cuma 'kitab'.
+        Default 'Hadir' supaya kolom absen tidak pernah null — kalau null,
+        baris ini tidak ikut dihitung di statistik kehadiran.
+        Ingat: absen === false berarti HADIR (ikut semantik kolom lama).
+      */}
+      <div>
+        <label className="block text-xs font-medium text-ink-700 mb-2">
+          Kehadiran
+        </label>
+        <div className="flex flex-wrap gap-2">
+          {[
+            { value: 'false', label: 'Hadir' },
+            { value: 'true', label: 'Tidak hadir' },
+          ].map((k) => (
+            <label key={k.value} className="cursor-pointer inline-flex">
+              <input
+                type="radio"
+                name="absen"
+                value={k.value}
+                className="peer sr-only"
+                defaultChecked={k.value === 'false'}
+              />
+              <span className="px-4 py-1.5 border border-line rounded-lg text-sm text-ink-700 peer-checked:bg-forest-700 peer-checked:text-cream-50 peer-checked:border-forest-700 transition">
+                {k.label}
+              </span>
             </label>
-            <div className="flex flex-wrap gap-2">
-              {[
-                { value: 'true', label: 'Hadir' },
-                { value: 'false', label: 'Tidak hadir' },
-              ].map((k) => (
-                <label key={k.value} className="cursor-pointer inline-flex">
-                  <input
-                    type="radio"
-                    name="absen"
-                    value={k.value === 'true' ? 'false' : 'true'}
-                    className="peer sr-only"
-                    defaultChecked={k.value === 'true'}
-                  />
-                  <span className="px-4 py-1.5 border border-line rounded-lg text-sm text-ink-700 peer-checked:bg-forest-700 peer-checked:text-cream-50 peer-checked:border-forest-700 transition">
-                    {k.label}
-                  </span>
-                </label>
-              ))}
-            </div>
-          </div>
+          ))}
+        </div>
+      </div>
 
+      {progresType === 'kitab' && (
+        <>
           <div className="grid sm:grid-cols-2 gap-4">
             <div>
               <label className="block text-xs font-medium text-ink-700 mb-1.5">
@@ -961,7 +967,7 @@ function ProgresCard({
                 {progres.lancar ? 'Lancar' : 'Tidak lancar'}
               </span>
             )}
-            {progresType === 'kitab' && progres.absen !== null && (
+            {progres.absen !== null && (
               <span
                 className={`text-[10px] font-medium uppercase tracking-wider px-2 py-0.5 rounded border ${
                   progres.absen === false
@@ -1160,37 +1166,38 @@ function ProgresEditForm({
         </div>
       )}
 
-      {progresType === 'kitab' && (
-        <>
-          <div>
-            <label className="block text-xs font-medium text-ink-700 mb-2">
-              Absen
+      {/* Kehadiran: tampil untuk semua kategori, sama seperti form setoran baru. */}
+      <div>
+        <label className="block text-xs font-medium text-ink-700 mb-2">
+          Kehadiran
+        </label>
+        <div className="flex flex-wrap gap-2">
+          {[
+            { value: 'false', label: 'Hadir' },
+            { value: 'true', label: 'Tidak hadir' },
+          ].map((k) => (
+            <label key={k.value} className="cursor-pointer inline-flex">
+              <input
+                type="radio"
+                name="absen"
+                value={k.value}
+                className="peer sr-only"
+                defaultChecked={
+                  progres.absen === null
+                    ? k.value === 'false'
+                    : String(progres.absen) === k.value
+                }
+              />
+              <span className="px-4 py-1.5 border border-line rounded-lg text-sm text-ink-700 peer-checked:bg-forest-700 peer-checked:text-cream-50 peer-checked:border-forest-700 transition">
+                {k.label}
+              </span>
             </label>
-            <div className="flex flex-wrap gap-2">
-              {[
-                { value: 'false', label: 'Hadir' },
-                { value: 'true', label: 'Tidak hadir' },
-              ].map((k) => (
-                <label key={k.value} className="cursor-pointer inline-flex">
-                  <input
-                    type="radio"
-                    name="absen"
-                    value={k.value}
-                    className="peer sr-only"
-                    defaultChecked={
-                      progres.absen === null
-                        ? k.value === 'false'
-                        : String(progres.absen) === k.value
-                    }
-                  />
-                  <span className="px-4 py-1.5 border border-line rounded-lg text-sm text-ink-700 peer-checked:bg-forest-700 peer-checked:text-cream-50 peer-checked:border-forest-700 transition">
-                    {k.label}
-                  </span>
-                </label>
-              ))}
-            </div>
-          </div>
+          ))}
+        </div>
+      </div>
 
+      {progresType === 'kitab' && (
+        <>
           <div className="grid sm:grid-cols-2 gap-4">
             <div>
               <label className="block text-xs font-medium text-ink-700 mb-1.5">